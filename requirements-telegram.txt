# Telegram Bot Requirements
# For the autonomous trading agent's Telegram interface

# Core Telegram Bot
python-telegram-bot[all]==21.0.1

# Scheduling and Timezone Support
pytz>=2024.1

# Async Support (included with python-telegram-bot, but listed for clarity)
httpx>=0.24.0
aiohttp>=3.8.0

# For Webhook/FastAPI Mode (optional, only needed for production)
# Uncomment these for webhook deployment
# fastapi>=0.100.0
# uvicorn[standard]>=0.23.0

# Database (already in main requirements, included for reference)
# sqlite3 is built-in with Python

# Environment Configuration
python-dotenv>=1.0.0

# Testing (development only)
pytest>=7.0.0
pytest-asyncio>=0.21.0
pytest-cov>=4.0.0
